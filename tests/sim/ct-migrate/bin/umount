#!/usr/bin/env bash
. "$SIMLIB"
m="${1%/}"; trace "umount $m"
grep -qxF "$m" "$SIMROOT/umount.fail" 2>/dev/null && { echo "umount: target is busy" >&2; exit 32; }
del_mount "$m"; del_loop_by_mnt "$m"; exit 0

# the live tables are per-site; only the samples are tracked
/ketsync.conf
/nodes.tsv
/fleet.tsv

# generated from the cluster, never edited by hand
/nodes.map

# runtime
/logs/
/state/

# engines/tp is committed, not cloned. Its own runtime leftovers - state/,
# logs/, pause/, the lock files and the real inventories - are ignored by
# engines/tp/.gitignore, which git applies relative to itself.

# editors and desktops
.DS_Store
._*
*.swp
*~
.idea/
.vscode/
